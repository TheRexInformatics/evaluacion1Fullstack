server:
  port: 8082

spring:
  application:
    name: pedidos-service

  datasource:
    url: jdbc:postgresql://localhost:5434/smartlogix_pedidos
    username: user_logix
    password: password123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha
    tagsSorter: alpha

logging:
  level:
    com.smartlogix.pedidos: DEBUG