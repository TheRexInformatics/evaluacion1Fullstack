server:
  port: 8082

spring:
  application:
    name: pedidos-service
  datasource:
    url: jdbc:postgresql://db-pedidos:5432/smartlogix_pedidos
    username: user_logix
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

inventario:
  service:
    url: ${INVENTARIO_SERVICE_URL:http://inventario-service:8081}

feign:
  circuitbreaker:
    enabled: true
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000