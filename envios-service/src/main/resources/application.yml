server:
  port: 8083

spring:
  application:
    name: envios-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  datasource:
    url: jdbc:postgresql://db-envios:5432/smartlogix_envios
    username: user_logix
    password: password123
    driver-class-name: org.postgresql.Driver